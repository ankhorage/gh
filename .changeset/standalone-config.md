---
'@ankhorage/gh': minor
---

Make GitHub connection configuration standalone by using the shared `RepositoryConfig` contract in `.ankhorage/gh.json` instead of requiring a complete `ankh.config.json`.
